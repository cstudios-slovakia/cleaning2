the client asked for some serious changes. here they are:
- not every task for a room  needes to be done every time
- every task should have an interval of days of when it should be done
- some tasks can be done just once for example cleaning windows
- so every room will have multiple task sets, with different intervals

- when a room is marked as done, the tasks should be reset based on the intervals
- one of the tasks whould be the quick clean and it should be applied when some someone initiates intervall cleaning.
- the task sest should be clonable, so when one is created it is copied and the copy can be modified
- the order of the task sets is important and should be maintained
- when a multiple task sets are triggered, the set lower on the list should be applied, because the more complex ones should be lower on the list.
